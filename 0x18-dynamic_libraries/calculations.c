int add(int a, int b)
{
	return (a + b);
}
int sub(int c, int d)
{
	return (c - d);
}

int mod(int c, int d)
{
	return (c % d);
}

int mul(int c, int d)
{
	return (c * d);
}
int div(int c, int d)
{
	return (c / d);
}
